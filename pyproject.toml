[project]
name = "econterm"
version = "0.0.1"
description = "a FRED research CLI with from-scratch regression analysis"
readme = "README.md"
requires-python = ">=3.12"
authors = [
    {name = "Ian Darby", email = "ian.lee.darby@gmail.com"}
]
dependencies = [
    "click",
    "httpx",
    "numpy",
    "matplotlib",
    "scipy",
    "python-dotenv",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "statsmodels",
    "respx",
    "mypy",
    "dotenv",
]

[project.scripts]
econterm = "econterm.cli:cli"
et = "econterm.cli:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"